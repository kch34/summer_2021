This folder contains the Reinforcement learning model.

--The Main point of this model is to have two robotic agents work together to move blocks on a 3x3 grid.
The agents Train for several game iterations to learn the best possible action set per game initialization